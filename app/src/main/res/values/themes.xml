<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.InterviewIN" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondaryContainer">@color/navigation_active</item>
    </style>

    <style name="Theme.InterviewIN" parent="Base.Theme.InterviewIN" />
</resources>